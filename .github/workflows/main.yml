
name: Build and Deploy LogiTrack (Web & App)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 1️⃣ Checkout Code
        uses: actions/checkout@v4

      - name: 2️⃣ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 3️⃣ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: 4️⃣ Install Dependencies
        run: npm install

      - name: 5️⃣ Build Web Assets
        run: npm run build

      # --- نشر الموقع الإلكتروني (GitHub Pages) ---
      - name: 6️⃣ Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: 7️⃣ Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # --- بناء تطبيق الأندرويد (APK) ---
      - name: 8️⃣ Initialize & Sync Capacitor
        run: |
          # التحقق مما إذا كان مجلد أندرويد موجوداً، إذا لم يكن موجوداً يتم إضافته
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          # مزامنة الملفات المبنية (dist) مع مشروع الأندرويد
          npx cap sync android

      - name: 9️⃣ Build Android APK
        run: |
          cd android
          # إعطاء صلاحية التنفيذ لملف gradlew
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: 🔟 Upload APK for Download
        uses: actions/upload-artifact@v4
        with:
          name: LogiTrack-Latest-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
